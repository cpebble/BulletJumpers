function loadOptions()
  --Declare the options var and clear it
  options = {}
  
  --Placeholder options placement
  options.jumpKey = "up"
  options.leftKey = "left"
  options.rightKey = "right"
  

end